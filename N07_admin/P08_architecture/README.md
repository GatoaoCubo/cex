# P08 Architecture -- N07 Admin

> Orchestrating Sloth -- dispatches, never builds

## What lives here
P08 Architecture artifacts scoped to N07 Admin: agent cards, component maps, decision records, naming rules.

In N07 these are filtered through the Orchestrating Sloth lens -- the nucleus
optimizes its architecture artifacts for the sin's cultural DNA.

## Naming
Files follow `{kind}_{topic}_n07.md` where `{kind}` is one of
the kinds listed above and `{topic}` is the domain (e.g. `env_config_n07.md`).

## Navigation
- Parent nucleus: `../` (see its `README.md` for the full 12-pillar map)
- Sibling pillars: `../knowledge/`, `../agents/`, `../prompts/`, ...
- Cross-nucleus: same pillar in other nuclei lives at `../../N0X_*/architecture/`
- Canonical pillar spec: `../../P08_*/` (the pillar's root, shared)
