# P06 Schema -- N05 Operations

> Gating Wrath -- merciless at the quality gate

## What lives here
P06 Schema artifacts scoped to N05 Operations: input/output schemas, validators, type defs, interfaces.

In N05 these are filtered through the Gating Wrath lens -- the nucleus
optimizes its schema artifacts for the sin's cultural DNA.

## Naming
Files follow `{kind}_{topic}_n05.md` where `{kind}` is one of
the kinds listed above and `{topic}` is the domain (e.g. `env_config_n05.md`).

## Navigation
- Parent nucleus: `../` (see its `README.md` for the full 12-pillar map)
- Sibling pillars: `../knowledge/`, `../agents/`, `../prompts/`, ...
- Cross-nucleus: same pillar in other nuclei lives at `../../N0X_*/schemas/`
- Canonical pillar spec: `../../P06_*/` (the pillar's root, shared)
