# CEX Coverage Report

## Overview
This report provides an overview of artifact coverage by pillar/kind for N05 operations. It measures the percentage of kinds in `kinds_meta.json` that have at least one built artifact and reports gaps so N05 can drive backfill.

## Usage
```bash
python _tools/cex_coverage.py
python _tools/cex_coverage.py --test
```