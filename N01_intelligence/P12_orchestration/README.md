# P12 Orchestration -- N01 Intelligence

> Analytical Envy -- envies every source to surpass it

## What lives here
P12 Orchestration artifacts scoped to N01 Intelligence: workflows, dispatch rules, schedules, crew templates, DAGs.

In N01 these are filtered through the Analytical Envy lens -- the nucleus
optimizes its orchestration artifacts for the sin's cultural DNA.

## Naming
Files follow `{kind}_{topic}_n01.md` where `{kind}` is one of
the kinds listed above and `{topic}` is the domain (e.g. `env_config_n01.md`).

## Navigation
- Parent nucleus: `../` (see its `README.md` for the full 12-pillar map)
- Sibling pillars: `../knowledge/`, `../agents/`, `../prompts/`, ...
- Cross-nucleus: same pillar in other nuclei lives at `../../N0X_*/orchestration/`
- Canonical pillar spec: `../../P12_*/` (the pillar's root, shared)
