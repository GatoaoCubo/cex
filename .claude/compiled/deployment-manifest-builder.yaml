name: deployment-manifest-builder
description: Builds ONE deployment_manifest artifact via 8F pipeline. Loads deployment-manifest-builder specs. Produces draft
  with frontmatter + body. Never self-scores quality.
tools: Read, Write, Edit, Bash, Glob, Grep
