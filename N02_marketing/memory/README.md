# P10 Memory -- N02 Marketing

> Creative Lust -- writes copy that seduces

## What lives here
P10 Memory artifacts scoped to N02 Marketing: entity memory, knowledge indexes, memory summaries, prompt caches.

In N02 these are filtered through the Creative Lust lens -- the nucleus
optimizes its memory artifacts for the sin's cultural DNA.

## Naming
Files follow `{kind}_{topic}_n02.md` where `{kind}` is one of
the kinds listed above and `{topic}` is the domain (e.g. `env_config_n02.md`).

## Navigation
- Parent nucleus: `../` (see its `README.md` for the full 12-pillar map)
- Sibling pillars: `../knowledge/`, `../agents/`, `../prompts/`, ...
- Cross-nucleus: same pillar in other nuclei lives at `../../N0X_*/memory/`
- Canonical pillar spec: `../../P10_*/` (the pillar's root, shared)
