id: p03_up_dispatch_agent_group
kind: user_prompt
action: dispatch_agent_group_task
input_required:
- task_description
- agent_group_name
- quality_target
output_expected: Completion signal + git commit with artifacts
pillar: P03
version: 1.0.0
created: 2026-03-24
author: orchestrator
domain: orchestration
quality: 9.2
tags:
- user-prompt
- dispatch
- handoff
- agent_group
- task
updated: '2026-04-07'
title: Action Prompt Dispatch Supervisor
density_score: 0.92
tldr: Defines user prompt for action prompt dispatch supervisor, with validation gates and integration points.
purpose: Instruct a agent_group to execute a specific task with defined inputs and quality gate.
input: "task: \"Create 8 P04 examples from real organization artifacts\"\nagent_group: edison\nquality_target: 9.0\nautonomy:\
  \ full\ncontext_files:\n  - \"C:/Users/PC/Documents/GitHub/cex/P04_tools/_schema.yaml\"\n  - \"C:/Users/PC/Documents/GitHub/cex/P04_tools/examples/ex_skill_cex_forge.md\""
execution:
- Read schema to understand type constraints (max_bytes, required frontmatter)
- Read source artifacts for real data (no invented content)
- Write each example matching template structure
- Validate size and frontmatter before saving
- Commit all files with descriptive message
output: 'git commit: "CEX: 8 P04 examples from real organization artifacts"

  signal: .claude/signals/edison_complete_9.0.json'
validation:
- Each file under type max_bytes
- All code fences balanced (even number of backtick fences)
- Frontmatter YAML parseable with required fields
- Body contains all template sections
- No placeholder text ({{VAR}} or PENDING/Configurable)
properties: '| Property | Value |

  |----------|-------|

  | Kind | `user_prompt` |

  | Pillar | P03 |

  | Domain | orchestration |

  | Pipeline | 8F (F1-F8) |

  | Scorer | cex_score.py |

  | Compiler | cex_compile.py |

  | Retriever | cex_retriever.py |

  | Quality target | 9.0+ |

  | Density target | 0.85+ |'
system_context: 'This artifact participates in the CEX typed knowledge system, a fractal

  architecture with 12 pillars, 8 nuclei, and 125 specialized builders.

  Artifacts flow through the 8F pipeline: Focus, Frame, Fetch, Filter,

  Format, Forge, Furnish, and Feedback.


  Quality is enforced via 3-layer scoring: structural (30%), rubric (30%),

  and semantic (40%). All artifacts target quality >= 9.0.


  | Layer | Weight | Method |

  |-------|--------|--------|

  | Structural | 30% | Automated count-based checks |

  | Rubric | 30% | Quality gate dimension scoring |

  | Semantic | 40% | LLM evaluation (when L1+L2 >= 8.5) |'
user_prompt_dispatch_agent_group_task: ''
quality_enforcement: This artifact follows the CEX 3-layer quality model. Structural scoring validates frontmatter completeness,
  content depth, and format diversity. Rubric scoring checks dimension-specific criteria from the quality gate. Semantic scoring
  provides LLM-based evaluation when layers one and two average 8.5 or above. All artifacts target quality 9.0 or higher.
  Below 8.0 triggers a rebuild. Between 8.0 and 9.0, the evolve pipeline applies targeted improvements.
