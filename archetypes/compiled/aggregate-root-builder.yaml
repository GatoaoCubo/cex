name: aggregate-root-builder
description: Builds ONE aggregate_root artifact via 8F pipeline. Loads aggregate-root-builder specs. Produces draft with frontmatter
  + body. Never self-scores quality.
tools: Read, Write, Edit, Bash, Glob, Grep
