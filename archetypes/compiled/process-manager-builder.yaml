name: process-manager-builder
description: Builds ONE process_manager artifact via 8F pipeline. Loads process-manager-builder specs. Produces draft with
  frontmatter + body. Never self-scores quality.
tools: Read, Write, Edit, Bash, Glob, Grep
