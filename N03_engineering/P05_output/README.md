# P05 Output -- N03 Engineering

> Inventive Pride -- refuses to ship mediocrity

## What lives here
P05 Output artifacts scoped to N03 Engineering: landing pages, formatters, parsers, diagrams, typed artifacts.

In N03 these are filtered through the Inventive Pride lens -- the nucleus
optimizes its output artifacts for the sin's cultural DNA.

## Naming
Files follow `{kind}_{topic}_n03.md` where `{kind}` is one of
the kinds listed above and `{topic}` is the domain (e.g. `env_config_n03.md`).

## Navigation
- Parent nucleus: `../` (see its `README.md` for the full 12-pillar map)
- Sibling pillars: `../knowledge/`, `../agents/`, `../prompts/`, ...
- Cross-nucleus: same pillar in other nuclei lives at `../../N0X_*/output/`
- Canonical pillar spec: `../../P05_*/` (the pillar's root, shared)
